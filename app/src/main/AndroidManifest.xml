<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".AddMedicineDetailsForm"></activity>
        <activity
            android:name=".TodayInfoActivity"
            android:label="Today's Medicines"

            android:parentActivityName=".addMedicineActivity" />
        <activity
            android:name=".searchMedicalshopActivity"
            android:label="Map"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".checkScheduleActivity"
            android:label="Check Daily Schedule"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".addMedicineActivity"
            android:label="Add Medicine"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>